# 9.10:  .groupby():  single-column aggregation.  First, view
# the DataFrame.  Then use .groupby() to show sum of
# 'SaleAmount' grouped by 'SalesRep'.

import pandas as pd

df = pd.read_excel("../sales-funnel.xlsx")

df.groupby(# your code here) # and here

