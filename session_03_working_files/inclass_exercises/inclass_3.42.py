# 3.42:  Add an image to the goodbye page.

# Add the happup.jpg image to the goodbye.html template.
# 
# In order to reference images in an HTML page served by
# flask, we must put the images in static/images and reference
# them using <IMG SRC="{{ url_for('static',
# filename='images/[name of image file]') }}">.
# 
# Expected Output:
# You should see the happup.jpg image along with the form
# input values

