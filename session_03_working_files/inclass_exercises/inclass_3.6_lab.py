# 3.6:  Retrieve a page and save to disk.

# Download the following page and save to disk in a new file
# (write response.text to the file).  Make sure to close the
# file.
# 
# After writing the file, please open the file in a browser
# (use your browser's File > Open File menu command).
# 
# In another tab on your browser, open the url directly.
# Compate the two pages.
# 
# (The locally saved file will not have correct formatting or
# colors - this is because these are supplied by a separate
# CSS file that was not downloaded.)

import runreport

import requests

url = 'https://pycoders.com/'


