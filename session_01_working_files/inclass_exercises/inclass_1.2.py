# 1.2:  Loop through a file and count lines.  Take the
# following steps, running the program and verifying the
# result after each step:

#   1. Loop through the file pyku.txt (located in the parent
# directory) and print each line.
#   2. Add an integer counter to count the lines, reporting
# the count at the end (3).
# 
# 

fname = '../pyku.txt'



